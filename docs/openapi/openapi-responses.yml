openapi: 3.0.3

info:
  title: base-responses
  version: v1

paths: {}

components:
  responses:
    user_response:
      content:
        application/json:
          schema:
            $ref: "openapi-schemas.yml#/components/schemas/user_response"
    http_common_response:
      content:
        application/json:
          schema:
            $ref: "openapi-schemas.yml#/components/schemas/http_response_common_dto"
